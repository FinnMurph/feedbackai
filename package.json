{
  "name": "feedbackai",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently --names \"flask,vite\" --prefix-colors \"cyan,magenta\" \"cd server && ./venv/bin/python3 app.py\" \"cd client && npm run dev\"",
    "install:client": "cd client && npm install",
    "setup": "npm run install:client"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
