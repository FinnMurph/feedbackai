# Optional: Add your Anthropic API key for live Claude responses.
# Without it, the app runs in demo mode with curated Socratic responses.
ANTHROPIC_API_KEY=

# Server config (defaults shown)
PORT=5001
FLASK_ENV=development
